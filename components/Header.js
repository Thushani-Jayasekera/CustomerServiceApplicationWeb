import React from "react";
import Navigation from "./Navigation";
const Header = ()=>{
  return(
    <header>
      <div className={"header"}>
        <Navigation/>
      </div>
    </header>
  )
}

export default Header